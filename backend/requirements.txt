# Zależności backendu (scrapery + wzbogacanie danych).
# Wersje odpowiadają środowisku deweloperskiemu (Python 3.13).
# Instalacja: pip install -r requirements.txt
supabase==2.28.3
python-dotenv==1.2.2
curl_cffi==0.15.0
aiohttp==3.13.5
